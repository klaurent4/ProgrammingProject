var userChoice; var randomNumber; var computerChoice;
userChoice = prompt("Heads or Tails?");
let randomNumber = (Math.random()>=0.5)? 1 : 0;
fucntion ;test (randomNumber) ;{
    if (randomNumber === 0) {
        return "Heads";
    }
    if (randomNumber === 1) {
        return "Tails";
    }
}
computerChoice = Heads;
computerChoice = Tails;
function equalityTest (userChoice) {
    if (userChoice == computerChoice) {
        return alert ('You guessed right! The coin flip landed on ',userChoice,'');
    }
    else
    return alert ('Sorry, the coin flip landed on ',userChoice,'');
}
var birthYear
birthYear = prompt ('Enter birth year')
currentYear = 2021
age = currentYear - birthYear
function test (age) {
    if (age > 21) {
        return alert("You are old enough to drink in the US") }
        if (age == 21) {
            return alert("You are old enough to drink in the US... barely")
        }
        if (age < 21) {
            return alert("Sorry, you are not old enough to drink in the US")
        }
}